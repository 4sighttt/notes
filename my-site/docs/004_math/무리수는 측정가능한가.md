
원의 둘레 L = $2\pi r$ 에서 $\pi$ = L / 2r

L = 1 이라고 하면 r 은 무리수가 된다고 한다


가로 세로 모두 1인 사각형의 대각선은 $\sqrt{2}$ 이다 

그런데 이건 정확한 측정이 안된다고 한다.

가로 세로 모두 측정 가능하지만

대각선인 $\sqrt{2}$는 측정이 안된다고 한다.

## 소수점 이하로 무한히 증가하지만 그 값은 정해져 있다.



